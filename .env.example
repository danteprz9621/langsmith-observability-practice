# LangSmith tracing + evaluate()/experiments -- free tier is enough for this project.
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your-langsmith-api-key-here
LANGCHAIN_PROJECT=langsmith-observability-practice

# Only required if tracing.py step 3 swaps rag_agent.py's Ollama call for a
# direct Anthropic client wrapped with wrap_anthropic.
ANTHROPIC_API_KEY=your-anthropic-api-key-here
