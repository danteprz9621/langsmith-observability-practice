langsmith>=0.1.0
# rag_agent.py unchanged from trailhead-travel-red-team: Ollama/Groq-served
# model + TF-IDF retrieval, no vector DB or embedding API.
ollama>=0.4.0
scikit-learn>=1.4.0
# Faithfulness (experiments.py, online_eval.py) needs an OpenAI-SDK-style
# client pointed at Ollama's or Groq's OpenAI-compatible endpoint, same
# pattern as trailhead-travel-rag-eval's tests/test_rag_agent.py -- not an
# actual OpenAI account.
ragas>=0.4.0,<1.0.0
openai>=1.0.0
langchain-community>=0.3.0,<0.4.0
# Currently unused -- kept in case agents/rag_agent.py's tracing ever routes
# through Anthropic's wrap_anthropic instead of tracing the existing
# Ollama/Groq call directly.
anthropic>=0.30.0
python-dotenv>=1.0.0
pytest>=7.0.0
